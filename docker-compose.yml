services:
  lectura-colaborativa:
    image: lectura-colaborativa:latest
    container_name: lectura-colaborativa-app
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "5000:5000"
    environment:
      - DB_HOST=192.168.18.2
      - DB_PORT=3306
      - DB_USER=feryna
      - DB_PASS=miPrincipe123$$
      - DB_NAME=feryna_collab_reader
      - PORT=5000
    volumes:
      - /DATA/AppData/lectura-colaborativa/uploads:/app/backend/uploads
    restart: unless-stopped
